{
  "name": "claude-manual-compactor",
  "version": "1.0.0",
  "description": "A script to split and compact files for Claude Code sessions",
  "main": "claude-manual-compact",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "start": "./claude-manual-compact"
  },
  "dependencies": {
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "jest": "^30.0.4"
  }
}
